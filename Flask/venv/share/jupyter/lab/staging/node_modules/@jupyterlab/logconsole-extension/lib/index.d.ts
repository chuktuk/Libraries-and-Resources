import { JupyterFrontEndPlugin } from '@jupyterlab/application';
import { ReactWidget } from '@jupyterlab/apputils';
import { ILoggerRegistry, LogConsolePanel } from '@jupyterlab/logconsole';
import * as React from 'react';
/**
 * A toolbar widget that switches log levels.
 */
export declare class LogLevelSwitcher extends ReactWidget {
    /**
     * Construct a new cell type switcher.
     */
    constructor(widget: LogConsolePanel);
    private _updateSource;
    /**
     * Handle `change` events for the HTMLSelect component.
     */
    handleChange: (event: React.ChangeEvent<HTMLSelectElement>) => void;
    /**
     * Handle `keydown` events for the HTMLSelect component.
     */
    handleKeyDown: (event: React.KeyboardEvent<Element>) => void;
    render(): JSX.Element;
    private _logConsole;
    private _id;
}
declare const _default: (JupyterFrontEndPlugin<void> | JupyterFrontEndPlugin<ILoggerRegistry>)[];
export default _default;
